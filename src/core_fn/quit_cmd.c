#include <main_header.h>

int quit_cmd(char* cmd, node_t* head)
{
    return 0;
    // if (my_strcmp(cmd, QUIT) == 0)
    // {
    //     //printf("%s\n",cmd);
        
    //     return -1;
    // }
    // return STDIN_FILENO;
}